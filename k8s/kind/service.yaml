apiVersion: v1
kind: Service
metadata:
  name: enterprise-rag-demo
  namespace: enterprise-rag
  labels:
    app: enterprise-rag-demo
spec:
  type: ClusterIP
  selector:
    app: enterprise-rag-demo
  ports:
    - name: http
      port: 80
      targetPort: http
