apiVersion: apps/v1
kind: Deployment
metadata:
  name: enterprise-rag-demo
  namespace: enterprise-rag
  labels:
    app: enterprise-rag-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: enterprise-rag-demo
  template:
    metadata:
      labels:
        app: enterprise-rag-demo
    spec:
      containers:
        - name: enterprise-rag-demo
          image: ghcr.io/fanslin/enterprise-rag-demo:sha-5a7268a2af91175bfe3b856050c318a35e3d0378
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: enterprise-rag-config
            - secretRef:
                name: enterprise-rag-secrets
                optional: true
          readinessProbe:
            httpGet:
              path: /api/health/ai
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /api/health/ai
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 3
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
